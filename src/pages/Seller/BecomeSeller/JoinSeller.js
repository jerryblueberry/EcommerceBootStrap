import React from 'react';
import './JoinSeller.css';
const JoinSeller = () => {
  return (
    <div>JoinSeller</div>
    //  gotta add the admin then admin gets a mail for each user registered to be seller and admin can look after each request in his dashboard
    //  seller needs to provide 3 documents and then admin valids it for now on 
    // after completion of the verification the email and the status changes to seller 
    
);
};

export default JoinSeller;
